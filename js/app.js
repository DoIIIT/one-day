$(function() {
    var options = {
        colorScale: colorScale,
        colorComb: colorComb,
        colorCombOrder: colorCombOrder,
        settings: setting3
    };
    var viz = new Viz(options);

    viz.play();
});
